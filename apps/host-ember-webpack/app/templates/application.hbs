<div class="hg">
  <header class="hg__header">
    <strong>host-ember-webpack</strong>
    <code>Ember {{this.emberVersion}}</code>
    <code>Embroider + webpack</code>
    <code data-testid="host-pathname">pathname={{this.router.currentURL}}</code>
  </header>

  <nav class="hg__nav" aria-label="Remotes">
    <h2>Remotes</h2>
    <LinkTo @route="index">Host home</LinkTo>
    <LinkTo @route="remote18">
      React 18
      <span class="nav-sub">remote18 / tanstack-router</span>
    </LinkTo>
    <LinkTo @route="remote19">
      React 19
      <span class="nav-sub">remote19 / tanstack-router</span>
    </LinkTo>
  </nav>

  <main class="hg__main">
    {{outlet}}
  </main>

  <footer class="hg__footer">
    Host owns the header and side nav. Nothing in the main area is host UI — the remote renders
    its own tabs, routes, and content.
  </footer>
</div>
