<nav class="tabs" aria-label="{{@remote}} routes">
  <LinkTo @route={{this.indexRoute}}>Home</LinkTo>
  <LinkTo @route={{this.catchallRoute}} @model="about">About</LinkTo>
  <LinkTo @route={{this.catchallRoute}} @model="items/7" class={{if this.isItems "active"}}>Items</LinkTo>
</nav>
